// pages/index/index.js — 首页
// Reviewer 约束:这个文件 ≤ ~30 行逻辑。generator 在后续 task 填充。

Page({
  data: {
    // TODO(g-1): 填充首页 data
  },

  onLoad() {
    // 启动时拉数据 — 由 generator 替换
  },

  onShow() {
    // 回到前台刷新 — 由 generator 替换
  },
});