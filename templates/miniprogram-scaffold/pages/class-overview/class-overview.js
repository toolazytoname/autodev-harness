// pages/class-overview/class-overview.js — 班级成绩页
// Reviewer 约束:这个文件 ≤ ~30 行逻辑。

Page({
  data: {
    // TODO(g-2): 班级列表 + 各班图表数据
  },

  onLoad() {
    // 启动时拉数据 — 由 generator 替换
  },

  onShow() {
    // 回到前台刷新 — 由 generator 替换
  },
});