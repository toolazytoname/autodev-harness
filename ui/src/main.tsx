import React from 'react';
import ReactDOM from 'react-dom/client';
import PetPalApp from './PetPalApp';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PetPalApp />
  </React.StrictMode>
);
